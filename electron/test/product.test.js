const ipcRenderer = require('electron').ipcRenderer;
const main = require('../dist/main');

//describe('Test product function', ()=>{
  //test('list productr', ()=>{
    //ipcRenderer.sendSync('list-product').then( ( data )=>{
      //isList = ( data ? true : false );
      //expect( true ).toBe(isList);
    //});
  //});
//});
